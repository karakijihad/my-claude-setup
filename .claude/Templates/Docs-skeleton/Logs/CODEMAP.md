# CODEMAP

> Authoritative file map for this project. Update after any structural change.

## Directory layout

```
src/
├── …
lib/
├── …
```

## Key files

| Path | Role | Lines | Notes |
|-|-|-|-|
| `src/index.ts` | entry point | — | — |

## Data flows

- **[Flow name]:** `entry → module → module → output`

## Conventions

- [e.g., "All HTTP handlers live in `src/routes/`, one file per resource."]
- [e.g., "Shared types live in `src/types/` — import from there, never redefine."]
